[build]
buildCommand = "npm ci && npm run server:build && node scripts/build.js"

[deploy]
startCommand = "npm run server:prod"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
